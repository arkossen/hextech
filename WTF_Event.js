var event = new WTFEventClass();

function init(loc, eventType, eventData, eventRange){
	event.loc = loc;
	event.eventType = eventType;
	event.eventData = eventData;
	event.eventRange = eventRange;
}

function Update () {
}